import React from 'react'; export default function Card({children}:{children:React.ReactNode}){return <div style={{background:'#fff',padding:16,borderRadius:12,boxShadow:'0 4px 16px rgba(0,0,0,.06)'}}>{children}</div>}